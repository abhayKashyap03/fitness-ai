# Archived sessions — 2026-07-19 to 2026-07-25

> Condensed handoffs moved out of `SESSION_LOG.md` (CLAUDE.md §6.3). `git log` is
> the authoritative history; this is orientation only.

## 2026-07-19 (c) — nutrition-source diagnosis
Read-only investigation: Apple Health is NOT a viable food source — MFP stopped
writing food to HealthKit after 2025-10-26 (Premium paywall), leaving 5 dietary
days total. Replan: food → a dedicated MFP path; HealthKit stays weight/body-comp.
Surfaced D4 (raw_events.source CHECK blocks a new source → §8.5 sign-off).

## 2026-07-19 (d) — Phase 5 WEIGHT (branch phase5/healthkit-weight)
D3 → ADR-0008. Migration 0005: non-destructive `source_app` + `utc_offset` on
`weight_measurement`/`food_entry`; resolver views rank OKOK scale over MFP-mirror.
HealthKit body ingest + pure normalizer (lb→kg, BodyFat%, LeanMass; §2.7). Verified
on the real 1.1 GB export: 1410 records → 298 resolved days (2023-07-06→2026-07-18).

## 2026-07-19 (e) — Phase 4 pre-work (branch phase4/coach-layer)
T4.1 `coach/tools.py` (5 model-callable tools over Phase-3 compute). §8.6
`compute/guardrails.py` (weight-loss-rate alert + 1200 kcal floor). T4.2
`coach/grounding.py` faithfulness prompt + fabrication scenarios (live eval gated).

## 2026-07-20 (a) — revamp session 1 (branch revamp/core-upgrades)
Correctness + friction: unit-aware HealthKit mass conversion (was unconditional lb),
per-migration atomic transactions, resp_rate enrichment on recovery, `coach sync`,
`coach doctor`, `db backup|verify`, `--json`. Multi-agent diff review → 9 fixes.
User's real DB confirmed: whoop_api 354 + healthkit 1410 raw rows.

## 2026-07-20 (b) — `coach ask` + provider-agnostic LLM (branch revamp/coach-ask)
Phase 4: bounded agent loop (MAX_ROUNDS=8) over the tool contract, stdlib-only
(no SDK). `run_live_grounding` implemented. Then refactored `llm.py` → `coach/llm/`
package: canonical shape in `base.py`, one module per vendor (google/anthropic),
`PROVIDERS` registry. **Default = Google Gemini** (free tier). Agent tests
parametrized across providers — the abstraction's proof.

## 2026-07-24 (a/b) — MyFitnessPal direct v2 API adapter (branch feat/mfp-adapter)
User signed off to override §12 for their own account (ADR-0010) and drop the
`raw_events.source` CHECK entirely (ADR-0009). Built `adapters/myfitnesspal/`
(cookie→bearer→v2 JSON, stdlib+httpx, zero new deps) + pure normalizer +
`upsert_food`. Migration 0006. **Live-reconciled** the diary shape: entries are
per-MEAL aggregates (`type:'diary_meal'`), filtered from exercise/steps to avoid
fabricating calories. Diary param fixed `date`→`entry_date`. MFP weight added;
migration 0007 ranks MFP below manual. WHOOP body-measurement ingest bug fixed.

## 2026-07-25 — Session-3 revamp overnight (branch feat/mfp-adapter, then main)
- MFP is the daily driver: `coach sync` = WHOOP + MFP (diary+weight) + normalize;
  HealthKit demoted to explicit `--hk-file` backfill.
- Gap-aware EWMA trend (ADR-0011, migration 0008, schema v8): effective alpha
  over a g-day gap = 1-(1-α)^g; ungapped history byte-identical. SQL view
  cross-validated against pure Python. Fixed a live correctness bug — the trend
  (and the TDEE/§8.6 guardrails on it) lagged for weeks after any travel gap.
- HRV validation harness (risk #6): `compute/hrv_validation.py` + `coach eval hrv`.
- BLE recon → ADR-0012: 5.0 protocol demonstrated (whoop-vault/NOOP, `fd4b`
  family); single BLE bond forces a TIME-SLICED calibration play; schema already
  models it (sibling rows). MG variant pending a one-evening hardware spike.
- README refresh.
- ⚠️ PROCESS NOTE: these 6 commits landed directly on `main` (the merged PR left
  the repo there and the branch wasn't re-checked). No history rewritten. Fix:
  branch-check is now step 1 of every session (saved to memory).
