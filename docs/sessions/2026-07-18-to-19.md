# Archived sessions — 2026-07-18 to 2026-07-19

Digest of completed work. Full detail in `git log`.

## 2026-07-18 (a) — Phases 0–3 (overnight autonomous)
- **Phase 0** migrations 0001 (base canonical: raw_events, recovery, workout,
  recovery_resolved), 0002 (food_entry + food_daily views), 0003 (weight_measurement
  + weight_resolved_daily + weight_trend EWMA α=0.10). ADR-0001/0002.
- **Phase 1** pyproject (src-layout), config.py (fail-loud typed Settings),
  store/db.py (numbered migration runner + schema_version, no Alembic),
  `coach db init|status`.
- **Phase 2** WHOOP: OAuth (auth.py/flow.py), client.py (v2 endpoints, next_token
  pagination, 429/5xx backoff), ingest.py (verbatim raw, payload_hash idempotent),
  normalize/whoop.py, `--rebuild` (byte-identical via canonical_fingerprint),
  dedup.py (session_group_id, ADR-0004), recovery_resolved resolver.
- **Phase 3** compute/daily.py (`coach status`), trends.py (EWMA/rolling/baseline,
  Insufficient below minimums), tdee.py (adaptive, ADR-0005, ±25 kcal convergence).

## 2026-07-18 (b) — Python 3.10 → 3.14 migration
Rebuilt .venv on 3.14.6; `requires-python>=3.14` (later relaxed to >=3.11), ruff/mypy
py314. UP017 `timezone.utc`→`datetime.UTC`. ADR-0003.
- **Heads-up (untouched):** `~/.zshrc` holds plaintext third-party API keys — flagged
  for the human to move out of the dotfile + rotate.

## 2026-07-18 (c) — align with hardened CLAUDE.md
- §3: floor relaxed `>=3.14`→`>=3.11` (floor = what code needs).
- §2.6/D1: migration 0004 adds `utc_offset`; `tz_name` now strictly IANA/NULL.
  ADR-0006.
- D2 → ADR-0007 (calorie precedence). DECISIONS_NEEDED queue emptied.

## 2026-07-18 (d) — live WHOOP ingest; fixed --since
Root cause of `GET /v2/recovery -> 404`: WHOOP v2 404s on a bare `YYYY-MM-DD`; needs
RFC3339. `WhoopClient._window` widens bare date to `T00:00:00.000Z`. **§10 first-contact
reconciliation PASSED** — real v2 field names match synthetic fixtures. Live recovery
normalized with `utc_offset=-04:00`, `tz_name` NULL.

## 2026-07-19 (a/b) — Phase 5 kickoff + T5.0–T5.2
- **T5.0** `apple_health_export/` gitignored, never tracked/staged/committed (verified
  every way). Export = 1.1 GB (later re-export 767 MB, current through today).
- **T5.1 recon** → `docs/healthkit-export-notes.md`. Nutrition SPARSE (~5 days);
  weight RICH (OKOK 296d + MFP 103d, unit **lb**); `HKTimeZone` IANA on dietary rows
  only.
- **T5.2 streaming parser** (`adapters/healthkit/parser.py`) — iterparse, flat memory,
  nutrition+body only, MetadataEntry read before clear. 7 tests. **PR #5** opened
  (T5.0–T5.2, still open).
- **D3 flagged** — HealthKit sub-source namespacing (see DECISIONS_NEEDED.md).
